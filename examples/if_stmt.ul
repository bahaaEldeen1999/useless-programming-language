int x  = 30;
int y = 30;
if(x == y)
{
    print("equals",string);
}
else
{
    if(x > y)
    {
        print("greater",string);
    }else 
    {
        if(x < y)
        {
            print("smaller",string);
        };
    };
};